import { Typography } from '@mui/material';
import React from 'react';

const Authors = () => {
    return (
      <>
        <Typography component="h3" variant='h5' fontWeight={600} fontSize={25} mb={3}>
          نویسنده ها
        </Typography>
      </>
    );
};

export default Authors;